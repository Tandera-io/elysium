/**
 * GameMap — renders the village layout overlays (paths, zone markers, etc.).
 * Currently a thin wrapper; visual details will be added as zones are built out.
 */
export function GameMap() {
  // Village boundary walls / paths can be added here as StaticProps grow.
  return null;
}
