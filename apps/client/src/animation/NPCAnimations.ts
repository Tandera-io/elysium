/** Frames-per-second for NPC walk animation cycles. */
export const NPC_ANIM_FPS = 6;

/** Minimum distance moved per frame (world units) to trigger the walk animation. */
export const NPC_MOVE_THRESHOLD = 0.001;
